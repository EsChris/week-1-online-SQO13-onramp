function fizzBuzz(input){
  if (input % 3 === 0) {
    return "fizz";
  }
  if (input % 5 === 0) {
    return "buzz";
  }
  if (input % 3 === 0 && input % 5 === 0) {
    return "fizzbuzz";
  }
  return input;
  let fizzBuzz = filterArray;
  if  ([4, "z", "f", 5]) {
    return (indexof.number);
  }
  if (["$%^", 567, "&&&"]) {
    return (number);
  }

  return input;
}

console.log(fizzBuzz(3)) //to return "fizz" 
console.log(fizzBuzz(5)) //to return "buzz" 
console.log(fizzBuzz(15)) // to return "fizz" 
console.log(fizzBuzz(7)) //to return 7 
console.log(filterArray([4, "z", "f", 5])) //to return [4,5]
console.log(filterArray(["$%^", 567, "&&&"])) // to return [567]

module.exports = fizzBuzz
